//- The sea paints underneath the map so it can animate on its own (see
//- SeaRenderer). Same z-index as the map, so DOM order keeps it behind.
canvas.map.map--sea#map-sea(aria-hidden='true')
canvas.map#map
